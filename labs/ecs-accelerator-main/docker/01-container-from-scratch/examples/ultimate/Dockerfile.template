# 1. Pick a base image
FROM <language>:<version>

# 2. Set work directory
WORKDIR /app

# 3. Copy dependency files first
COPY <dependency files> .

# 4. Install deps
RUN <install command>

# 5. Copy the rest
COPY . .

# 6. Expose a port (optional) - this is just for docs, remember...
EXPOSE <port>

# 7. Run the app
CMD ["<runtime>", "<app>"]